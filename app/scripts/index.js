$(document).ready(function() {

$("form").submit(function() {
	console.log("it worked");
					var gladiator = $(".character").val();
					var arena = $(".arena").val();
					var weapon = $(".weapon").val();
 
					if(gladiator === "Laser") {
 
						if(arena === "Gauntlet") {

							if(weapon === "giantMetalBall")
 
						
 
							gauntlet.begin(laser);
							$(".status").text(laser.life )
						}
 
 
					}
 if(gladiator === "laser") {
 
						if(arena === "Eliminator") {

							if(weapon === "giantMetalBall")
 
						
 
							gauntlet.begin(laser);
							$(".status").text(laser.life )
						}
 
 
					}
if(gladiator === "laser") {
 
						if(arena === "Gauntlet") {

							if(weapon === "tennisBallGun")
 
						
 
							gauntlet.begin(laser);
							$(".status").text(laser.life )
						}
 
 
					}

if(gladiator === "laser") {
 
						if(arena === "Eliminator") {

							if(weapon === "tennisBallGun")
 
						
 
							gauntlet.begin(laser);
							$(".status").text(laser.life )
						}
 
 
					}

					if(gladiator === "Thunder") {
 
						if(arena === "Gauntlet") {

							if(weapon === "giantMetalBall")
 
						
 
							gauntlet.begin(Thunder);
							$(".status").text(Thunder.life )
						}
 
 
					}
 if(gladiator === "Thunder") {
 
						if(arena === "Eliminator") {

							if(weapon === "giantMetalBall")
 
						
 
							gauntlet.begin(Thunder);
							$(".status").text(Thunder.life )
						}
 
 
					}
if(gladiator === "Thunder") {
 
						if(arena === "Gauntlet") {

							if(weapon === "tennisBallGun")
 
						
 
							gauntlet.begin(Thunder);
							$(".status").text(Thunder.life )
						}
 
 
					}

if(gladiator === "Thunder") {
 
						if(arena === "Eliminator") {

							if(weapon === "tennisBallGun")
 
						
 
							gauntlet.begin(Thunder);
							$(".status").text(Thunder.life )
						}
 
 
					}


			}); }


